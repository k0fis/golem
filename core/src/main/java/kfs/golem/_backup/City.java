package kfs.golem._backup;


public class City  {
    /*
    private ParallaxSystem parallax;
    private InteractionSystem interact;

    public City(GolemMain main) {
        super(main);
    }

    @Override
    public void show() {
        parallax = new ParallaxSystem(stage);
        parallax.addLayer("backgrounds/sky.png", 0.2f);
        parallax.addLayer("backgrounds/prague_mid.png", 0.5f);
        parallax.addLayer("backgrounds/prague_front.png", 1.0f);

        play("sounds/morning.mp3");
        interact = new InteractionSystem(stage);

        // ✨ Interaktivní objekty
        var lamp = interact.addObject("objects/lamp.png", 680, 200);
        lamp.setSound("sounds/lamp_on.ogg");
        lamp.setOnInteract(() -> lamp.fadeHighlight());

        var cat = interact.addObject("objects/cat.png", 300, 150);
        cat.setSound("sounds/cat_meow.ogg");
        cat.setOnInteract(() -> {
            setSubtitle("Ticho. Jen kočka šeptá do noci.");
        });

        play("sounds/morning.mp3", 06f);

        setSubtitle("Město se probouzí. Dýchá pomalu...");
    }

    @Override
    public void render(float delta) {
        super.render(delta);
        parallax.update(delta);

        // jednoduchý fade přechod po pár sekundách
        if (time > 30f) {
            game.setScreen(new FadeTransition(game, this, new Laboratory(game)));
        }
    }

     */

}
